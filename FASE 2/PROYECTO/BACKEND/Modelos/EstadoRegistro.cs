namespace BACKEND.Modelos
{
    /// <summary>
    /// Estados de registros maestros, alineados con el ENUM de MySQL (ACTIVO / INACTIVO).
    /// </summary>
    public enum EstadoRegistro
    {
        ACTIVO,
        INACTIVO
    }
}

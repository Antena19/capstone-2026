namespace BACKEND.Modelos
{
    /// <summary>
    /// Estado del registro pasajero-servicio, alineado con el ENUM de MySQL (ACTIVO / CANCELADO).
    /// </summary>
    public enum EstadoPasajeroServicio
    {
        ACTIVO,
        CANCELADO
    }
}
